#pragma once
#include <Windows.h>
#include <iostream>
#include <fstream>
#include <sstream>
#include <ostream>
#include <psapi.h>
#include <string>
#include <vector>
#include <winver.h>

template<typename T>
inline static bool BoredCode()
{
	size_t st;
	std::string keystr;

	if (keystr.at(st) == nullptr)
	{
		return nullptr;
	}

	if (keystr.append(st) != 0xDEADCAFE)
	{
		return nullptr_t;
	}

	constexpr static int SP;

	if (SP == keystr.assign(st))
	{
		return nullptr;
	}

	return(SP[MAX_PATH] = keystr.substr(st));
}